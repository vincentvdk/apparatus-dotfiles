# UWSM environment variables (common to all sessions)
# https://github.com/Vladimir-csp/uwsm

# XDG
export XDG_CURRENT_DESKTOP=Hyprland
export XDG_SESSION_TYPE=wayland
export XDG_SESSION_DESKTOP=Hyprland
# Include user flatpak apps in search path (for walker/elephant)
export XDG_DATA_DIRS="$HOME/.local/share/flatpak/exports/share:${XDG_DATA_DIRS:-/usr/local/share:/usr/share}"

# Qt
export QT_QPA_PLATFORM=wayland;xcb
export QT_QPA_PLATFORMTHEME=qt6ct
export QT_WAYLAND_DISABLE_WINDOWDECORATION=1
export QT_AUTO_SCREEN_SCALE_FACTOR=1

# GTK
export GDK_BACKEND=wayland,x11,*

# Firefox
export MOZ_ENABLE_WAYLAND=1

# Cursor
export XCURSOR_SIZE=24
export HYPRCURSOR_SIZE=24

# Terminal (for SSH compatibility with terminals like Rio)
export TERM=xterm-256color
